apiVersion: v1
kind: Service
metadata:
  name: airflow-statsd
  namespace: airflow
spec:
  selector:
    app: airflow-webserver
  ports:
    - name: metrics
      port: 9102
      targetPort: 9102
  type: ClusterIP
