apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: allow-airflow-ui
  namespace: airflow
spec:
  podSelector:
    matchLabels:
      app: airflow
  ingress:
  - ports:
    - protocol: TCP
      port: 8080