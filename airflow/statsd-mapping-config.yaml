apiVersion: v1
kind: ConfigMap
metadata:
  name: statsd-mapping
  namespace: airflow
data:
  mapping.yaml: |
    mappings:
      - match: "airflow.*"
        name: "$1"
        labels:
          component: "airflow"
